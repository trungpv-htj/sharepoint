import { OnInit, Renderer } from '@angular/core';
import { ActivatedRoute, Router } from '@angular/router';
export declare class NgxSidebarComponent implements OnInit {
    private renderer;
    private router;
    private route;
    navigation: Array<any>;
    containerClass: string;
    state: boolean;
    constructor(renderer: Renderer, router: Router, route: ActivatedRoute);
    ngOnInit(): void;
    menuToggle(event: any, nav: any): void;
}
